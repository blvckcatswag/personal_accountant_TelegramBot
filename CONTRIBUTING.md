# Contributing

Use Python 3.12+, run `pip install .[dev]`, keep async code typed and add tests for new business logic.

Workflow:
- create a feature branch from `main`
- run `ruff check .` and `pytest`
- keep PRs small and focused
- do not commit secrets or `.env`

