# User Guide

Commands:

- `/start` register and see the quick help
- send a receipt photo, document or text dump
- `/stats week` or `/stats month` for analytics
- `/history` for the latest receipts
- `/budget` to create a budget limit
- `/currency UAH` to change the base currency
- `/mydata` to export personal data
- `/deleteaccount` to erase all stored data

